# Minimal requirements for Flask web application
# Core web framework
flask>=2.3.0
python-dotenv>=1.0.0

# Database connectivity
mysql-connector-python>=8.0.0

# HTTP requests and API calls
requests>=2.31.0

# Discord OAuth (lighter alternative to discord.py)
# We'll use requests for OAuth instead of the full discord.py library

# Basic data processing (much lighter than pandas/numpy)
# Only install if you specifically need data analysis features

# Image processing (only if you need image manipulation)
# Pillow>=10.0.0

# Security
cryptography>=41.0.0

# Date/time utilities
python-dateutil>=2.8.0

# JSON handling improvements
simplejson>=3.19.0
